import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root') // o App é um componente, que está sendo renderizado pela função ReactDom.render() dentro do elemento 'root' do Html.
);

